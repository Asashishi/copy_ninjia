# dependencies (bun install)
node_modules

# Google Cloud service account credentials
g-auth.json

# output
out
dist
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# AI memory snapshots + daily luck cache (diskIOWorker, same privacy tier as logs)
memory

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# deployment configuration (copy and customize config_example/)
# 必须锚定根目录：裸 `config` 会匹配任意层级的同名目录，把 packages/config/ 与
# test/config/ 下的新增源码一并静默忽略（既有文件因已被跟踪而不受影响，问题只
# 在新增时暴露）。
/config

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# AI local settings
.claude
.agents
.codex

# 本地临时的任务/审查笔记，不进版本库
todo.md
plan.md
review.md

# Finder (MacOS) folder config
.DS_Store

# runtime state (regenerated on start, not source)
bot.lock
bot.lock.*
state.json
state.json.bak
state.json.*.corrupt
.state.json.*.tmp
