# 10.0.2 全仓核对与发布计划

## 结论

- 全仓实现、引用关系、性能、模块边界和项目约定已完成复核；当前门禁范围内未发现遗留问题。
- 生产消息热路径、状态机顺序、权限语义、持久化格式和 Worker 协议没有功能或语义漂移。
- `install.sh` 只在复制到 `/tmp` 的隔离 PTY 夹具中执行；真实 `config/`、运行时数据、服务账号和部署文件未被安装测试读写。

## 当前边界

- Telegram 与 Agent 部署配置严格拒绝示例占位凭据，错误只报告文件路径、字段路径和期望形态。
- 安装器对现有配置采用外部备份、哈希清单、严格解析和同目录原子替换；保留软链接目标、权限与属主，不覆盖有效部署配置。
- 问答直答只按 Telegram entity 判定真实提及；模块、缓存 owner、后台任务和 Bun/Node API 边界由约定门禁核对。
- 全量基准和热路径门禁为每个子进程创建独立配置及运行时数据根；Telegram 与模型出站均由基准守卫替换，结束后清空 `performance/` 临时目录。

## 实测证据

| 检查 | 结果 |
| --- | --- |
| `bun run check` | 通过；2,898 pass、0 fail、96,591 次断言、295 个文件；包含安装隔离、规范、lint、typecheck、覆盖率与热路径门禁 |
| `bun run test:fault-injection` | 通过；452 pass、0 fail、3,458 次断言、36 个文件 |
| 安装隔离 | 8 组场景通过；外部命令为白名单伪实现，未触发网络、提权、systemd、journal 或生产入口 |
| 热路径硬门禁 | Bun 1.4.0；10/10 场景通过，每场 3 次重复，`softLatencyReports=[]`，GC 最大占比 0%，最大采样 RSS 128,942,080 bytes（门限 402,653,184） |
| 全量性能基准 | 默认 3 轮通过；8 条端到端链路和 28 个热路径/容器场景；报告时间 `2026-08-29T12:06:41Z` |
| 引用与模块 | 2,815 个导出声明的未引用扫描为 0；生产模块图、文件规模、缓存 owner 和层级约定通过 `check:conventions` |
| 依赖与数据边界 | `bun.lock`、`bunfig.toml` 和依赖列表无变化；部署配置、SQLite、状态、内存目录和锁文件无版本差异 |

全量基准摘要：启动就绪总耗时均值 497.951 ms；消息主干 1.333 µs/op；入群日志追加 413.825 ops/s；广告检测命令 150.333 ops/s；AI 回复命令 857.166 ops/s。完整三轮数据由同一次 `--write-doc` 写入三语 `09-performance.md` 和 `performance-result.json`。

## 发布序列

1. 在 `dev` 保持上述代码、文档和结构化报告同树，并确认工作树无残留。
2. 以 `git merge --squash` 生成 `master` 的单次发布提交并推送。
3. 单独创建并推送 annotated tag `10.0.2`，再创建并核验 GitHub Release。
4. Release 成功后将 `dev` 对齐到 `master`，核验本地与远端引用一致，并观察服务两个重启间隔。
